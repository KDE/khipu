kde4_install_icons( ${DATA_INSTALL_DIR}/khipu/icons)

#no funciona lo siguiente lo recomento aaron y en el top un include icons y listo
# no la idea es tenerlos dentro de data gplacs

#kde4_install_icons( ${ICON_INSTALL_DIR} )

